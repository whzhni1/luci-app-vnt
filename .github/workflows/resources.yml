name: List Files

on:
  workflow_dispatch:

env:
  REPO: https://github.com/immortalwrt/luci.git
  BRANCH: master

jobs:
  list:
    runs-on: ubuntu-latest
    steps:
      - name: Clone and List Files
        run: |
          # 稀疏克隆
          git clone --depth 1 --filter=blob:none --sparse $REPO repo
          cd repo
          
          # 匹配所有 bootstrap 相关主题
          git sparse-checkout set themes/luci-theme-bootstrap themes/luci-theme-bootstrap-light themes/luci-theme-bootstrap-dark
          
          echo "========== 📁 文件结构 =========="
          find themes -type f | sort
          
          echo ""
          echo "========== 🔗 原始链接 =========="
          for file in $(find themes -type f | sort); do
            echo "https://raw.githubusercontent.com/immortalwrt/luci/$BRANCH/$file"
          done
